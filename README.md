# React Pattern
    The following are the react pattern
## 1- Controlled and Uncontrolled Component
    Control Component:
        In a react, a controlled component is a form input elements like inputs,select,button,textarea etc whose value controlled by react state rather then dom (dom means using useRef hook)